{
  "name": "@flowforge/compat-tests",
  "version": "0.0.1",
  "private": true,
  "author": "Damir Manapov",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:official": "BASE_URL=http://localhost:3001/api/v1 TARGET_NAME=official vitest run",
    "test:reimpl": "BASE_URL=http://localhost:4000/api/v1 TARGET_NAME=reimpl STUB_TOKEN_DELAY_MS=10 vitest run",
    "test:record": "RECORD_GOLDENS=1 BASE_URL=http://localhost:3001/api/v1 TARGET_NAME=official vitest run"
  },
  "dependencies": {
    "@flowforge/test-utils": "workspace:*",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0"
  }
}
